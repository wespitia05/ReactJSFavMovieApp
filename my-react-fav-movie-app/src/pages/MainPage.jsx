import SearchBar from "../components/SearchBar";

export default function MainPage() {
    return (
        <div className="movie-app">
            <SearchBar />
            <p>Main page (we’ll add popular movies/shows next)</p>
        </div>
    );
}
